# ⚡ Быстрое решение: Если что-то не работает

Ошибка `NetworkError when attempting to fetch resource` обычно имеет одну из 3 причин:

## ☑️ Чек-лист (делай по порядку)

### Шаг 1: API ключ сохранился? (30 сек)

Открой **DevTools** (F12) → вкладка **Console** → скопируй:

```javascript
localStorage.getItem('gemini_api_key')
```

**Результат:**
- ✅ Если видишь `"AIzaSy..."` → идем на Шаг 2
- ❌ Если видишь `null` → перезагрузи страницу (F5), введи ключ заново

---

### Шаг 2: Правильный ли ключ? (1 мин)

**Ключ должен:**
1. Начинаться с `AIza`
2. Быть длинной ~39 символов
3. Быть активным (не удаленным в Google AI Studio)

**Если неправильный:**
```javascript
localStorage.removeItem('gemini_api_key');
// Перезагрузи страницу (F5) и введи новый ключ
```

**Получи новый ключ:** https://makersuite.google.com/app/apikey

---

### Шаг 3: Есть ли интернет?

- ✅ Проверь что интернет работает
- ✅ Попробуй открыть https://google.com
- ✅ Если на VPN → отключи и попробуй снова

---

### Шаг 4: Смотри логи (1 мин)

Нажми на "Search" в приложении и сразу открой **Console** (F12).

Ищи эти сообщения:

**✅ Если видишь:**
```
📡 Sending request to Gemini API with key: AIza...
📊 Response status: 200
✅ Response received: {...}
```
→ **ВСЕ РАБОТАЕТ!** 🎉

**❌ Если видишь один из этих ошибок:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `API key not valid` | Ключ неправильный | Создай новый в Google AI Studio |
| `API key not set` | Ключ не сохранился | Перезагрузи F5, введи заново |
| `401 Unauthorized` | Ключ неавторизован | Удали старый, создай новый |
| `429 Too Many Requests` | Лимит запросов | Подожди 5 минут |
| `Failed to fetch` | Нет интернета или VPN | Проверь интернет, отключи VPN |
| `CORS policy` | Блокировка браузером | На Vercel должно работать |

---

## 🔥 Самое вероятное (90% случаев):

**Ты используешь неправильный или скомпрометированный ключ.**

### Что делать:

1. Перейди на https://console.cloud.google.com/
2. В "APIs & Services" → "Credentials" найди свой ключ
3. Нажми на него → **"Edit"** → **"Delete"**
4. Создай новый ключ на https://makersuite.google.com/app/apikey
5. Используй новый ключ в приложении

---

## 💡 Продвинутая отладка

Скопируй это в **Console** (F12) и выполни:

```javascript
// 1. Проверь ключ
const key = localStorage.getItem('gemini_api_key');
console.log('📍 Ключ:', key ? key.substring(0, 20) + '...' : 'НЕТ');

// 2. Попробуй запрос вручную
fetch('https://generativelanguage.googleapis.com/v1beta/models/gemini-pro:generateContent?key=' + key, {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    contents: [{
      parts: [{ text: 'Hello' }]
    }]
  })
})
  .then(r => {
    console.log('📊 Status:', r.status);
    return r.json();
  })
  .then(d => console.log('✅ Response:', d))
  .catch(e => console.error('❌ Error:', e.message));
```

**Смотри результаты в Console ниже** ⬇️

---

## ❓ Если все еще не работает?

Напиши сообщение об ошибке из **Console** (F12):
- Точный текст ошибки
- Статус код (если есть)
- Результат проверки ключа (Шаг 1)

Пример:
```
Ошибка: "API key not valid"
Ключ: AIzaSyABC123...
Status: 400
```

---

**ВАЖНО:** На Vercel/Netlify это должно работать автоматически! 🚀

Если локально работает, значит когда залишь на хостинг, все будет норм! ✅

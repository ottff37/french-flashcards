# 🚀 Гайд по развертыванию на Vercel/Netlify

Этот гайд поможет тебе развернуть приложение в интернете за несколько минут.

## Что нужно перед тем как начать

✅ GitHub репозиторий с кодом (уже готов)  
✅ Аккаунт на Vercel или Netlify (бесплатно)  
✅ Gemini API ключ (бесплатно, от Google)  

## Развертывание на Vercel (рекомендуется) ⭐

Vercel - это компания, которая создала Next.js и специально оптимизирована для React приложений.

### Шаг 1: Подготовка GitHub

Убедись что твой репозиторий залит на GitHub:

```bash
git add .
git commit -m "Initial commit"
git push origin main
```

### Шаг 2: Подключение к Vercel

1. Перейди на [vercel.com](https://vercel.com)
2. Нажми **"Sign Up"** и выбери **"Continue with GitHub"**
3. Авторизуйся и разреши доступ
4. Нажми **"Import Project"**
5. Выбери репозиторий `french-flashcards`

### Шаг 3: Конфигурация проекта

Vercel автоматически обнаружит что это Vite React приложение.

**Проверь настройки:**
- Build Command: `npm run build` ✅
- Output Directory: `dist` ✅
- Install Command: `npm install` ✅

Если все правильно, нажми **"Deploy"** 🟢

### Шаг 4: Готово!

Через 1-2 минуты приложение будет доступно по ссылке:
```
https://french-flashcards.vercel.app
```

Vercel автоматически пересоберет приложение при каждом пуше в main ветку!

---

## Развертывание на Netlify

Netlify - альтернатива Vercel, тоже отличный вариант.

### Шаг 1: Подключение к Netlify

1. Перейди на [netlify.com](https://netlify.com)
2. Нажми **"Sign up"** и выбери **"GitHub"**
3. Авторизуйся через GitHub
4. Нажми **"Import an existing project"**
5. Выбери репозиторий `french-flashcards`

### Шаг 2: Конфигурация

Netlify покажет форму для конфигурации:

**Build settings:**
- Build command: `npm run build`
- Publish directory: `dist`

Нажми **"Deploy"** 🟢

### Шаг 3: Готово!

Через 1-2 минуты приложение будет доступно.

Ссылка будет выглядеть как:
```
https://french-flashcards-abc123.netlify.app
```

---

## Использование API ключа в продакшене

### Важно!

**Каждый пользователь должен использовать свой собственный API ключ!**

Как это работает:
1. Пользователь открывает приложение
2. Появляется модальное окно: **"Gemini API Key"**
3. Пользователь вводит свой собственный ключ (бесплатный от Google)
4. Ключ сохраняется в localStorage его браузера
5. Все запросы идут с его ключом

### Получение бесплатного API ключа (для каждого пользователя)

1. Перейти на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Нажать **"Create API Key"**
3. Выбрать проект (или создать новый)
4. Скопировать ключ (начинается с `AIza`)
5. Вставить в модальное окно приложения

**Важно:** Google дает **60 запросов в минуту** для свободного тарифа. Этого достаточно для личного использования.

---

## Переменные окружения (опционально)

Если захочешь добавить переменные окружения на Vercel/Netlify:

### На Vercel:

1. Перейди в Settings проекта
2. В левом меню найди **"Environment Variables"**
3. Добавь переменные

### На Netlify:

1. Перейди в Site settings
2. В левом меню: **"Build & deploy" → "Environment"**
3. Добавь переменные

**Пример (если понадобится):**
```
VITE_API_URL=https://example.com
```

Потом в коде использовать:
```javascript
const apiUrl = import.meta.env.VITE_API_URL;
```

Но для текущего приложения это не нужно!

---

## Проверка что все работает

После развертывания:

1. Откройся по ссылке приложения
2. Должно появиться модальное окно с просьбой API ключа
3. Введи свой Gemini ключ
4. Попробуй добавить слово
5. Если перевод появился — все работает! ✅

---

## Проблемы при развертывании

### Ошибка при сборке

**Проблема:** "npm run build failed"

**Решение:**
1. Проверь что все зависимости установлены: `npm install`
2. Убедись что нет ошибок синтаксиса в коде
3. Проверь package.json - там должны быть react и react-dom

### Приложение не открывается

**Проблема:** 404 ошибка при открытии

**Решение:**
1. Убедись что build выполнился успешно (смотри логи)
2. Очисти кэш браузера (Ctrl+Shift+Delete)
3. Проверь что у тебя последняя версия кода на GitHub

### API запросы не работают

**Проблема:** "NetworkError when attempting to fetch resource"

**Решение:**
1. Проверь что ты ввел правильный API ключ
2. Убедись что интернет работает
3. Открой DevTools (F12) → Console и смотри логи
4. Если нужна помощь - смотри `TROUBLESHOOTING_NETWORK_ERROR.md`

---

## Автоматические обновления

После развертывания, приложение будет автоматически пересобираться при каждом пуше:

```bash
# Вносишь изменения локально
git add .
git commit -m "Add new feature"
git push origin main

# Vercel/Netlify автоматически пересобирает!
# Через 1-2 минуты изменения будут в продакшене
```

---

## Собственный домен (опционально)

Если хочешь использовать свой домен вместо vercel.app или netlify.app:

### На Vercel:

1. Settings → Domains
2. Добавь свой домен
3. Следуй инструкциям для настройки DNS

### На Netlify:

1. Settings → Domain
2. Добавь свой домен
3. Следуй инструкциям для настройки DNS

---

## Масштабирование

Если у тебя много пользователей и хочешь оптимизировать:

1. **Кэширование:** Включи кэширование на CDN (обе платформы делают это автоматически)
2. **Аналитика:** Добавь Google Analytics или Plausible
3. **Мониторинг:** Следи за ошибками через Sentry
4. **Платный план:** Если нужна больше мощности - переходи на платные тарифы

---

## Помощь и поддержка

**Документация:**
- Vercel: https://vercel.com/docs
- Netlify: https://docs.netlify.com
- Vite: https://vitejs.dev/guide/

**Если что-то не работает:**
1. Проверь логи (в интерфейсе Vercel/Netlify есть раздел "Logs")
2. Открой DevTools (F12) на развернутом сайте
3. Проверь Console на ошибки
4. Попробуй локально: `npm run dev`

---

## Готово! 🎉

Теперь твое приложение работает в интернете и все могут им пользоваться!

**Поздравляю с развертыванием!** 🚀✨
